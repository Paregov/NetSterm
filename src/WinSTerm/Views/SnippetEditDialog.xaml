<mah:MetroWindow x:Class="WinSTerm.Views.SnippetEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        Title="New Snippet"
        Width="450" Height="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        TitleCharacterCasing="Normal"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        BorderThickness="1">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Name *" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NameBox"
                     mah:TextBoxHelper.Watermark="e.g. Check disk usage"/>
        </StackPanel>

        <!-- Command -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Command *" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="CommandBox"
                     AcceptsReturn="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Height="100"
                     mah:TextBoxHelper.Watermark="e.g. df -h"
                     FontFamily="Consolas"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Description" Margin="0,0,0,4"/>
            <TextBox x:Name="DescriptionBox"
                     mah:TextBoxHelper.Watermark="Optional description"/>
        </StackPanel>

        <!-- Category -->
        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="Category" Margin="0,0,0,4"/>
            <TextBox x:Name="CategoryBox"
                     mah:TextBoxHelper.Watermark="e.g. Monitoring, Cleanup"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" MinWidth="80" Margin="0,0,8,0"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Click="OkButton_Click"/>
            <Button Content="Cancel" MinWidth="80"
                    Style="{DynamicResource MahApps.Styles.Button.Square}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
