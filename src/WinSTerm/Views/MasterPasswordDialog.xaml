<mah:MetroWindow x:Class="WinSTerm.Views.MasterPasswordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
    Title="WinSTerm - Master Password"
    Width="400" Height="220"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    ShowInTaskbar="True"
    TitleCharacterCasing="Normal"
    Icon="/Assets/winsterm.ico"
    GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
    BorderThickness="1">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Enter master password to unlock WinSTerm:"
                   FontSize="14"
                   Margin="0,0,0,12"/>

        <PasswordBox Grid.Row="1"
                     x:Name="PasswordField"
                     mah:TextBoxHelper.Watermark="Master Password"
                     Margin="0,0,0,8"
                     KeyDown="PasswordField_KeyDown"/>

        <TextBlock Grid.Row="2"
                   x:Name="ErrorText"
                   Foreground="{StaticResource ErrorBrush}"
                   FontSize="12"
                   Visibility="Collapsed"
                   Margin="0,0,0,8"/>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Unlock"
                    MinWidth="80"
                    Margin="0,0,8,0"
                    Style="{DynamicResource MahApps.Styles.Button.Square.Accent}"
                    Click="Unlock_Click"/>
            <Button Content="Exit"
                    MinWidth="80"
                    Style="{DynamicResource MahApps.Styles.Button.Square}"
                    Click="Exit_Click"/>
        </StackPanel>
    </Grid>
</mah:MetroWindow>
